# Scenario: Postgres failover

Kill the primary in a Patroni-managed Postgres cluster and observe automated
promotion of a replica. Validates HA behavior of a stateful workload.

**Status**: not yet documented
